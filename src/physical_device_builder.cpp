#include <dragon/core.hpp>

namespace Dragon {
    PhysicalDeviceBuilder::PhysicalDeviceBuilder(Instance const& instance) {
        this->instance = instance;
    }

    Result<PhysicalDevice> PhysicalDeviceBuilder::buildFirst() {
        VkPhysicalDeviceCreateInfo
    }
}