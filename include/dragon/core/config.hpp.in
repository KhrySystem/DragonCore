#pragma once

#include <vulkan/vulkan.hpp>
#include <boost/predef.h>

#define DRAGON_ENGINE_NAME "DragonEngine"
#define DRAGON_ENGINE_VERSION VK_MAKE_API_VERSION(@DragonEngine_VERSION_TWEAK@, @DragonEngine_VERSION_MAJOR@, @DragonEngine_VERSION_MINOR@, @DragonEngine_VERSION_PATCH@)
#define VERSION(tweak, major, minor, patch) VK_MAKE_API_VERSION(tweak, major, minor, patch)

